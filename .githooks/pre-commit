#!/usr/bin/env bash
set -euo pipefail
BAD=$(git diff --cached --name-only | grep -E '\.l0\.dev\.json$' | grep -v '/build/' || true)
if [ -n "$BAD" ]; then
  echo "Refusing to commit dev artifacts outside build/:" >&2
  echo "$BAD" >&2
  exit 1
fi
