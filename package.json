{
	"name": "tf-lang",
	"private": true,
        "scripts": {
                "test": "vitest run docs/claims-explorer.test.ts && pnpm run --recursive test",
                "build": "pnpm run --recursive build",
                "preinstall": "npx only-allow pnpm"
        },
        "devDependencies": {
                "typescript": "^5.5.3",
                "vitest": "^2.0.5",
                "jsdom": "^24.0.0"
        },
        "pnpm": {
                "allowScripts": {
                        "esbuild": true
                }
        }
}