{
  "name": "@tf-lang/adapter-execution-ts",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "pnpm --filter tf-lang-l0 run build",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "prefixtures": "pnpm run build",
    "fixtures": "node dist/artifacts.js",
    "preparity": "pnpm run build",
    "parity": "node dist/parity.js"
  },
  "dependencies": {
    "@tf-lang/utils": "workspace:*",
    "tf-lang-l0": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "typescript": "^5.5.0",
    "vitest": "^2.1.3"
  }
}
