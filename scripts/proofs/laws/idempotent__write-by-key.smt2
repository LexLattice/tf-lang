(declare-sort IdKey 0)
(declare-sort Key 0)
(declare-sort URI 0)
(declare-sort Val 0)
(declare-fun W (URI Key IdKey Val) Val)
(declare-const u URI)
(declare-const k Key)
(declare-const ik IdKey)
(declare-const v Val)
(define-fun once () Val (W u k ik v))
(define-fun twice () Val (W u k ik (W u k ik v)))
(assert (not (= twice once)))
(check-sat)
